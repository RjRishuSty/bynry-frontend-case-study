import React from 'react';
import { Typography, Box } from '@mui/material';

const AdminDashboard = () => (
  <Box p={4}>
    <Typography variant="h4">Admin Dashboard (Coming Soon)</Typography>
    {/* Here, you'll manage add/edit/delete profiles */}
  </Box>
);

export default AdminDashboard;